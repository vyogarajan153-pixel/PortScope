{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install dependencies",
            "type": "shell",
            "command": "python -m pip install -r requirements.txt"
        },
        {
            "label": "Run PortScope",
            "type": "shell",
            "command": "python app.py"
        },
        {
            "label": "Run tests",
            "type": "shell",
            "command": "pytest"
        }
    ]
}
